# Manual Correction Brief (VERIFIED against source 2026-08-23)

Fix `reference/manual/*.md` (01-08 + Archive/08-native-mode.md) per this fact base.
Constraints (from user):
- The default way to use the app is the way it runs on the Pi. NEVER say "native mode" or "non-native mode" or "browser mode" — just describe the behavior as the app's standard operation. (Keep "Open from Pi" button name — that's a literal UI label.)
- If a shortcut/feature does not exist in code, DO NOT document it. No "planned" notes.
- Keep the existing figure references (`![...](./figures/...)`) exactly as they are — screenshots are being captured separately.
- Do not touch `reference/manual/figures/`, `SCREENSHOT_CAPTURE_GUIDE.md`, or `scripts/`.
- Write concise, accurate, plain prose. Match the existing tone (short lines, bullets, tables where present).

## VERIFIED FACT BASE

### Toolbar (frontend/src/components/Toolbar.tsx)
Buttons (visible when applicable): **Import**, **Open from Pi**, **Export**, **Save**, **Revert**, **Diff**, **AI Chat**, **Flash**, **Component** (add menu), **Macro**, **Manual**, **Text View**, and a **gear** ("Customize top bar") that lets users hide/show toolbar items (persisted to localStorage).
- Import, Export always available. Open from Pi / Save / Revert / Diff / Flash appear when a project is loaded / originals exist / native.
- Save button color = whole-project validation state: **grey** (no changes), **green** (valid, unsaved changes), **yellow** (warnings), **red** (errors block save). See utils/saveButtonClass.ts.
- Save and Revert/Diff appear when originals exist (after Open from Pi or a save).

### Keyboard shortcuts — ACTUAL (only these exist in code)
- Graph view: `Ctrl+Z` undo, `Ctrl+Y` / `Ctrl+Shift+Z` redo (App.tsx). **Nothing else.** There is NO F, NO Delete, NO Ctrl+D, NO Ctrl+A, NO F1, NO Ctrl+S, NO Ctrl+Shift+S, NO Ctrl+F, NO Ctrl+T bindings in the app.
- Text View: `Escape` closes the search panel; `Enter` confirms rename dialogs. NO Ctrl+F/Ctrl+G/Ctrl+//Ctrl+Space.
- AI Chat: `Enter` sends, `Shift+Enter` newline (ChatDialog). NO Ctrl+Enter, NO Ctrl+L/H/,
- Macro Designer: `Delete`/`Backspace` deletes the selected canvas item (when not typing in an input). Simulation uses on-screen buttons (Play/Stop, Step back/forward, Reset), NOT shortcuts. NO Ctrl+S/Ctrl+Enter/Ctrl+D/Ctrl+Shift+S.
- Firmware dialog: `Escape` closes open popovers.
Fix EVERY keyboard shortcut table in 01, 02, 03, 05, 06, 08 to list only what exists. In 01 the appendix shortcuts table: remove the whole table or replace with a one-liner "Undo/redo: Ctrl+Z / Ctrl+Y (graph view)".

### Import dialog (ImportDialog.tsx)
- Drop zone: "Drop config files, a folder, or a .zip archive here" + subtext about printer.cfg + includes. Buttons: **Select Files**, **Select Folder**, **Cancel**.
- After picking: file list with checkboxes ("Deselect any you don't want to import"), then a confirm button. If selected files already exist in the project → overwrite confirmation dialog.
- Import MERGES into the current project (never clears). Stages files as unsaved changes (no disk write, originals untouched).
- There is NO "project summary showing MCU count" step. Don't invent one.

### Open from Pi (OpenFromPiDialog.tsx)
- Title "Open from Pi", subtitle "Read Klipper config files directly from the Raspberry Pi".
- Config Directory input + **Refresh** button. File list with checkboxes + **All**/**None** links. Footer: two checkboxes "Clear existing config" (default ON) and "Clear Macro Designer drafts/layout" (default ON), **Cancel**/**Done**, and primary button **"Open N Files"**.
- Klipper SAVE_CONFIG backup files (`printer-YYYYMMDD_HHMMSS.cfg`) are filtered out of the list.
- Sets originalTexts from disk → Save/Revert/Diff become available.

### Graph view (App.tsx, graphStore.ts, AddMenu.tsx, nodes/*)
- Nodes: SBC, Mainboard (PRIMARY badge on the primary mainboard), Toolhead, Expander, Probes, Accelerometers (hardware nodes auto-collapse when nothing inside is selected).
- **There is NO right-click context menu in the graph.** Node actions are the small **Duplicate** (copy icon) and **Delete** (trash icon) buttons on each node (NodeActions.tsx). Hardware-node delete shows a confirm popover ("Delete {name} and all attached configuration sections?").
- **There is NO "Group Selected" / "Ungroup" menu.** Grouping happens automatically: dragging a node onto a sibling of the same component group merges them into a group node (addGroupNode). Children of groups can be dragged back out (removes from group). Do NOT document a Group Selected menu.
- **There is NO "Primary MCU toggle"** that renames files or rewrites pin prefixes. isPrimary is just a display badge (thicker border, PRIMARY label). Do NOT document a primary toggle workflow.
- Adding hardware (Component button → "Major Components" tab): SBC, Mainboard, Toolhead, Expander, Probe, Accelerometer, Other. For mainboard/toolhead/expander/probe/accelerometer the menu shows a **template picker** ("Blank {label}" option + a search list of example configs from the backend). Selecting a template creates the node from the example; "Blank" creates an empty section.
- Adding sub-components ("Sub-Components" tab): groups Steppers, Stepper Drivers, Extruders, Heaters, Fans, Temperature Sensors, Probes, LEDs, Displays, Servos, Output Pins, Filament Sensors, Accelerometers, MCU. Added to the currently selected node (or standalone if none).
- "Features" tab: Bed Leveling (bed_mesh, z_tilt, quad_gantry_level...), Homing (safe_z_home...), Resonance (input_shaper, resonance_tester), G-Code Features (virtual_sdcard, pause_resume, gcode_macro...). Optional "Attach to component" picker.
- **MCU name prompt** (McuNameDialog.tsx): adding a NON-PRIMARY board prompts "Non-primary boards need an MCU name. This name is used in section headers (e.g., [mcu EBBCan]) and as a pin prefix (e.g., EBBCan:gpio13)." — this IS real.
- Edges: drag from a node's handle to another node to create/adjust the connection; edge type is inferred from section params (USB/UART/CAN). Selecting an edge shows a panel with "Delete link".
- Validation badges on nodes: green/yellow/red roll-up of section validation (WarningBadge).
- Pan/zoom: background drag pans, scroll wheel zooms, React Flow toolbar (Zoom In/Out, Fit View) bottom-left; **Arrange** button top-left of canvas auto-lays out.

### Settings panel (SettingsPanel.tsx)
- Opens when a node is selected. Shows section name (editable inline — node rename), parameter fields (text/number/dropdown/pin per schema), red/yellow validation highlights, "Reference link" style docs access, add/remove parameter rows. When an edge is selected instead: include relation + "Delete link".

### Text view (TextEditor.tsx)
- Layout: LEFT file sidebar (Files: list with error/warning dots, + "Add Configuration" button, collapse), CENTER editor (textarea with line numbers, validation underlines; parse-failure banner when text can't parse — "the model holds last-good; Save blocks until this clears"), RIGHT sections sidebar (section TOC with error/warning dots, collapse).
- **Live sync: 800ms debounce** — parses typed text and applies to the graph model; on parse failure the last-good model is held and a banner shows.
- **Reference viewer is a MODAL DIALOG** (ConfigReferenceDialog), opened from the editor toolbar "Configuration Reference" button — NOT a right sidebar. It searches bundled Klipper docs.
- **Search: "Search all files…" panel** toggled from the editor toolbar (Escape closes).
- **File context menu: RIGHT-CLICK a file in the left sidebar** → **Rename** (disabled for printer.cfg), **Duplicate**, **Delete** (disabled for printer.cfg, confirm dialog). Renaming rewrites `[include]` references across the project.
- Add Configuration dialog: "Blank config" (name input) or "From Reference" (search example configs).
- NO keyboard shortcuts for search/goto/autocomplete.

### Save / Apply dialog (ApplyDialog.tsx)
- Opens from **Save** (toolbar). Left: file list with checkboxes + per-file badges (deleted/new file/unchanged/N lines changed). Right: unified diff per selected file.
- Footer: **Cancel** (becomes **Done** on success), **Apply** button whose color mirrors the toolbar Save state (grey/green/yellow/red). Parse errors in text view block Apply with a message.
- On success (native path): files written to the configured config path; then the dialog shows a **"Firmware Restart"** button ("Restart Sent" after). It polls Klipper status; shows "Recent Klipper errors" list + log path on failure; warns "Moonraker reports an active print job ... Firmware restart is disabled until it finishes."
- **There is NO "restart now / restart later / don't restart" choice** — it's a single Firmware Restart button after save. Do not document a three-option restart dialog.
- **There is NO persistent Klipper status bar** in the app. Klipper status appears inside the Apply dialog after save, and the AI chat / toolbar don't show one. Remove any "status bar shows Connected/Disconnected/Restarting/Recent errors" description. (In Archive/08: the status states Ready/Starting/Printing/Error are what the /klipper-status API can report — phrase it as "the Save dialog reports Klipper status after a restart" instead of a status bar.)
- Backups: KWC does NOT create `backup_[timestamp]/` directories. Klipper's SAVE_CONFIG creates `printer-YYYYMMDD_HHMMSS.cfg` files in the config directory; Open from Pi filters those out of its file list. Document THAT, not a backup dir.

### Diff dialog (DiffDialog.tsx)
- Title "Changes Since Import". Left sidebar: every file with badges (new / changed+count / deleted / unchanged dot). Right: unified diff (+ green, - red, @@ blue hunk headers, context grey). "No changes — identical to the imported version." when clean. **Close** button. There is no prev/next-change navigation and no click-line-to-jump.

### Export dialog (ExportDialog.tsx)
- Title "Export Configuration". Left: file checkboxes with per-file error/warning counts + expandable issue lists. Right: diff panel (only when originals exist) showing changed lines per selected file. Footer: **Format** select — "Individual files (.cfg)" or "ZIP archive (.zip)" — **Cancel**, **Export N file(s)** / **Export ZIP (N file(s))**.

### Revert dialog (RevertDialog.tsx)
- Simple confirm dialog: reverts the whole project (no per-file selection). Native path: re-reads files from disk. Otherwise: re-parses stored originals ("No original versions to revert to." error if none). Has "Clear Macro Designer drafts/layout" checkbox like Open from Pi. Message "Reverting changes..." then success.

### AI Chat (ChatDialog.tsx, ChatSettingsPanel, AiDraftPreviewDialog, ChatMessageList)
- Providers in the dropdown (aiStore.ts): **google, chatgpt, anthropic, github, openai-compatible**. (These are the real option values — OpenAI=chatgpt, Google, Anthropic Claude, GitHub Copilot, OpenAI-Compatible.) So the provider list in the manual CAN be specific; use those.
- Settings fields: API key (cloud only; openai-compatible needs a key for https URLs), model (fetched from /v1/models for http endpoints), host/port (openai-compatible), max tokens, temperature (0-2), tool protocol (auto/native/text).
- Input bar: **Enter sends, Shift+Enter newline** (NOT Ctrl+Enter). Stop button while generating. Paperclip to attach files. "Include Files" menu to check which loaded files are context.
- Tool use: model calls MCP tools; footer badges show which tools ran.
- Draft preview: when an assistant message contains cfg edits and a file is active, the bubble shows **"Apply and Review Changes"** → opens the draft preview dialog with per-file tabs, unified diff coloring, per-file Accept; files with validation errors block acceptance. After accept, changes stage into the project (Save/Export to persist).
- Printer memory: icon in title bar ("View and edit printer memory"); AI proposes memory blocks; review dialog.
- Conversation save/load: history dialog (clock icon "View and load past conversations").
- There is NO "Test Connection" button. Don't document one.

### Flash Tool (FirmwareDialog.tsx — 2188 lines, fully verified)
- Dialog tabs: **Klipper** / **Katapult** (Katapult tab only shown when Katapult is installed/detected — per user intent).
- Workflow: select target → **menuconfig preview** (live preview of the relevant menuconfig fields; "Changes update the visible menuconfig fields immediately"; "No visible menuconfig options are available for the current selection." when none) → **Save** (writes the active .config file, then offers to store the setup as a named profile) → **Build** → **Flash**.
- **Flash Profiles DO EXIST**: "Save writes the active .config file and then lets you store the current flash setup under a unique host-side profile name. Load opens the active config or any saved host-side flash profile for the current target." Profile dialog: name input, list with **Load**/**Delete** per profile. Stored in `~/.config/klipper-wire-configurator/flash_profiles/` (per-target subdirs, .json). RESTORE a "Flash Profiles" section in 07 describing this accurately (profile name, Load/Delete, host-side storage).
- Settings gear: override Klipper/Katapult checkout paths + **Refresh detected flash devices** (USB DFU, serial, CAN UUIDs).
- Flash devices: auto-detection (USB DFU 0483:df11, serial, CAN UUIDs, RP2040 boot); method auto-matched to device with manual override (methods include dfu-util, make flash, flashtool).
- Build: local build with progress; build log shown on failure.
- The "Kconfig Editor" section of the OLD manual was about a menuconfig preview — retitle it "Menuconfig Preview" and describe it as above (no "Edit Kconfig" button; it's a live field preview).

### Macro Designer (MacroDesignerDialog.tsx)
- Opens from **Macro** button. Tabs/panels: macro list (search, templates), G-code editor for the selected macro, machine canvas (toolhead position, bed, no-go zones, dock position), simulation controls.
- Simulation: **Play/Stop** button ("Stop simulation, clear seeded commands, reset toolhead to centered/cold"), **Step back**, **Step forward**, **Reset**. No-go zones drawn on the canvas; selected zone can be moved/resized; **Delete/Backspace** deletes the selected canvas item.
- Machine profile is derived from the loaded config (travel bounds etc.).
- NO keyboard shortcuts beyond Delete/Backspace on canvas items.

### File locations (backend/services/native_services.py)
- KWC state dir: `~/.config/klipper-wire-configurator/` (override with KWC_LAYOUT_DIR) containing `layout.json`, `settings.json`, `flash_profiles/`.
- **flash.log is at `backend/flash.log`** (backend directory), NOT in the config dir. Fix the appendix table.
- Config dir: default `~/printer_data/config/` (from the native status).

## PER-FILE FIX LIST

**01-getting-started.md**
- Toolbar table: fine, but remove "(Native mode)" style annotations if any remain; keep button list as-is (Import, Open from Pi, Export, Save, Revert, Diff, AI Chat, Flash, Component, Macro, Manual, gear).
- "From Your Pi" steps must match OpenFromPiDialog: config directory + Refresh, checkboxes, "Open N Files" button, Clear existing config checkbox.
- "From Your Computer" steps must match ImportDialog: Select Files / Select Folder / drag-drop, checkbox selection, overwrite confirm, merge behavior. Remove the invented "project summary showing MCU count".
- Delete the whole "Appendix: Keyboard Shortcuts" table; replace with: only `Ctrl+Z`/`Ctrl+Y` undo/redo exist (graph view).

**02-graph-ui.md**
- Remove "Fit to Screen — Double-click the background or press F" → just React Flow Fit View button + scroll zoom + background drag pan. Remove "Auto-Arrange — Auto-Layout button" → it's the **Arrange** button.
- Adding Components: fix to the actual 3-tab menu (Major Components / Sub-Components / Features) + template picker + blank option + MCU name prompt for non-primary boards. Remove "right-click a parent node" (no context menu) — it's the Component (+) toolbar button, and sub-components attach to the currently selected node.
- Reparenting: drag node onto another node (drop preview shows "Drop Into {name}" with Features/Components columns).
- Remove the "Grouping and Ungrouping" section's "Group Selected"/"Ungroup" menu → replace: groups form automatically when you drag a component onto a sibling of the same type; drag a child out to ungroup.
- Remove the "Primary MCU Toggle" section entirely.
- Validation/Pin conflict sections: keep but verify wording (pin conflicts surface as validation errors in the settings panel / node badges).
- Keyboard shortcuts table: only Ctrl+Z / Ctrl+Y.
- Node types appendix: keep (matches).
- Figure references: keep all as-is.

**03-text-ui.md**
- Layout: LEFT = Files sidebar, CENTER = editor, RIGHT = Sections sidebar (TOC). The "Reference Viewer (Right Sidebar)" section must become "Configuration Reference" MODAL dialog (toolbar button), per TextEditor.tsx.
- Live Sync: keep (800ms verified) — but say parse failure holds last-good + banner; Save/Apply blocked until fixed.
- Keyboard shortcuts table: remove all entries except none — the editor has no shortcuts (Escape closes search; Enter confirms dialogs). Replace table with a note or delete the section.
- File Operations: right-click file in the left Files sidebar → Rename / Duplicate / Delete (printer.cfg cannot be renamed or deleted). "Add Configuration" (+ button in Files header) → Blank config or From Reference.
- Search: "Search all files…" toolbar panel (Escape to close). No Ctrl+F.
- Switching Between Files: files are rows in the left sidebar (not tabs) — click a file to edit it. Remove "tabs" language and Ctrl+Tab shortcuts.
- Remove Ctrl+T planned note.

**04-save-diff-export.md**
- Save button table: 4 states — grey (no changes), green (valid changes), yellow (warnings), red (errors block).
- Save dialog: per-file checkboxes + badges (new file/deleted/unchanged/N lines changed) + per-file diff; Apply button color mirrors toolbar; parse-error block message; after success → **Firmware Restart** button (single action, not 3 options) + "Recent Klipper errors" panel + active-print warning.
- Remove ALL "restart now / later / don't restart" three-option content.
- Remove "status bar" section (no persistent status bar).
- Backups: replace `backup_[timestamp]/` with Klipper SAVE_CONFIG `printer-YYYYMMDD_HHMMSS.cfg` files in the config directory; Open from Pi filters them; to restore, copy the SAVE_CONFIG file back.
- Revert: whole-project revert (no per-file selection), "Clear Macro Designer drafts/layout" checkbox, native re-reads from disk / otherwise re-parses originals.
- Diff viewer: "Changes Since Import", sidebar badges, unified diff; no prev/next navigation; no click-to-jump.
- Export: file checkboxes + error/warning counts, format select (Individual files / ZIP archive), "Export N file(s)".
- "Saving Changes" section: remove "Prerequisites: You're in Native Mode" language; phrase as "when KWC has a config directory (Open from Pi / Save available)".
- Fix "native mode" / "browser mode" phrasing throughout.

**05-macro-designer.md**
- Keyboard shortcuts table: only Delete/Backspace (selected canvas item). Remove Ctrl+S/Ctrl+Enter/Ctrl+D/Ctrl+Shift+S/Ctrl+/.
- Simulation section: Play/Stop + Step back/forward + Reset buttons (not shortcuts).
- Verify figure refs stay (fig-1-designer-open.svg, fig-5-2-designer-layout.png, fig-3-motion-preview.svg, fig-4-no-go-zones.svg).

**06-ai-chat.md**
- Provider table: use the real dropdown options: OpenAI (chatgpt), Google (gemini), Anthropic (claude), GitHub Copilot, OpenAI-Compatible (local/any OpenAI-compatible endpoint incl. LM Studio, Ollama, llama.cpp, cloud https).
- "Testing the Connection": remove the "Test Connection" button mention (doesn't exist).
- Keyboard shortcuts: Enter sends, Shift+Enter newline. Remove Ctrl+Enter/Ctrl+L/Ctrl+H/Ctrl+,.
- "Apply and Review Changes" button on the assistant bubble → draft preview. Per-file accept; validation errors block acceptance.
- Keep Printer Memory + MCP tools + history sections (accurate), fix any "browser mode" wording.

**07-flash-tool.md**
- RE-ADD "Flash Profiles" section (verified real): Save stores the current setup under a unique host-side profile name; Load opens it; profiles stored in `~/.config/klipper-wire-configurator/flash_profiles/`; per-profile Load/Delete in the profile dialog.
- Replace "Kconfig Editor" section with "Menuconfig Preview": live preview of menuconfig fields for the selected board; changes update fields immediately; "No visible menuconfig options..." when none; Save writes the active .config.
- Katapult tab: only shown when Katapult is present.
- Flash devices: detected USB DFU / serial / CAN UUIDs / RP2040; settings gear for checkout paths + refresh devices; method auto-match with manual override.
- Build: local build, progress, log on failure.
- Remove anything about "Flash via USB — drag the .uf2 when prompted" IF the UI doesn't say that; RP2040: the make-flash method handles it. Keep the drag-and-drop explanation as a general RP2040 fact but attribute the action to the flash method, not a KWC "drag when prompted" button.
- "Source Version" build error row: keep (checking `git branch` in the checkout dir is the right advice) but phrase checkout path (settings gear shows/overrides it).

**08-appendix.md**
- Keyboard shortcuts: collapse to what exists (graph: Ctrl+Z/Ctrl+Y; macro canvas: Delete/Backspace; chat: Enter/Shift+Enter; text: Escape closes search). Delete the rest.
- flash.log location → `backend/flash.log` (next to the backend code), not the config dir.
- Keep file locations table (layout.json, settings.json, flash_profiles/) with the correct config dir.

**Archive/08-native-mode.md**
- Remove "Klipper Connection Status" table describing a status bar (none exists) → rephrase: "The Save dialog reports Klipper status after a restart (Ready / error states via the Klipper API; recent error lines + log path on failure)."
- Keep serial paths, config paths, UART section (enable_uart=1 — correct).
- Backups: SAVE_CONFIG `printer-YYYYMMDD_HHMMSS.cfg` files, not backup dirs.
