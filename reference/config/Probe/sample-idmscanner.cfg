Ok great, lets add some more logic now.

I want this tool to pull down the readme files from all the GitHub pages and store the content in a file "toolhead_scan.temp".

Once they have been pulled in, parse the content for any information about the following:

- Compatible Hotends
- Compatible Extruders
- Compatible Probes
- Compatible Toolhead Boards

You can use the extruders.json, hotends.json, and probes.json files for examples of what to look for.

If you find any content related to it, pull the JSON from toolheads.json for that toolhead and see if any of those parsed items are written in as compatible. 
If not, generate a new file called "Updated_Toolheads.json" that contains the same content and format as toolheads.json for the toolhead that needs updated.

Since you likely will find similarly named but not identically named hotends, extruders, and probes, create an alias.json that allows you to map multiple names back to a single name.
Use this alias.json to help with parsing and creating "Updated_Toolheads.json". 

In the terminal, if any toolhead needs update, create a relevant log that tells you which toolhead needs updated and what it found.
If nothing is found, just have the terminal say "No Updates Needed"

When everything is done, delete toolhead_scan.temp.